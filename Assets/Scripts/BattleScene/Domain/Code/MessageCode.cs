namespace BattleScene.Domain.Code
{
    public enum MessageCode
    {
        NoMessage, // メッセージ未設定
        BleedingMessage, // 状態異常(出血)
        BurningMessage, // 状態異常(燃焼)
        ConfusionActMessage, // 状態異常(混乱)
        EnemyConfusionActMessage, // 状態異常(敵混乱)
        ParalysisCantActMessage, // 状態異常(麻痺)
        PoisoningMessage, // 状態異常(中毒)
        SuffocationMessage, // 状態異常(窒息)
        AilmentMessage, // 共通(状態異常)
        AttackMessage, // 共通(攻撃)
        AvoidMessage, // 共通(回避)
        BeatEnemyMessage, // 共通(敵死亡)
        BuffMessage, // 共通(バフ)
        DamageMessage, // 共通(与ダメ)
        DamageOneselfMessage, // 共通(自身に与ダメ)
        DebuffMessage, // 共通(デバフ)
        DefenceMessage, // 共通(防御)
        FailAilmentMessage, // 共通(状態異常失敗)
        IsContinueMessage, // 共通(継続選択)
        PlayerDeadMessage, // 共通(死亡)
        PlayerWinMessage, // 共通(戦闘勝利)
        RecoverAilmentMessage, // 共通(状態異常回復)
        RecoverDestroyedPartMessage, // 共通(損傷部位回復)
        RemoveAilmentMessage, // 共通(状態異常解除)
        RestoreHitPointMessage, // 共通(HP回復)
        RestoreTechnicalPointMessage, // 共通(TP回復)
        SelectTargetMessage, // 共通(敵選択)
        SkillMessage, // 共通(スキル) TODO: 削除すること
        SlipDamageMessage, // 共通(スリップダメ)
        EnemySuicideMessage, // 共通(敵混乱死亡)
        TryAttackMessage, // 共通(攻撃後回避)
        WeakPointMessage, // 共通(弱点)
        AfterimageMessage, // ダメージ(残像)
        BiteArmMessage, // ダメージ(噛みつき)
        BiteLegMessage, // ダメージ(噛みつき)
        BiteOffArmMessage, // ダメージ(食いちぎり)
        BiteOffLegMessage, // ダメージ(食いちぎり)
        BiteOffStomachMessage, // ダメージ(食いちぎり)
        BiteStomachMessage, // ダメージ(噛みつき)
        CutArmMessage, // ダメージ(命を刈る鎌)
        CutLegMessage, // ダメージ(命を刈る鎌)
        CutStomachMessage, // ダメージ(命を刈る鎌)
        CutUpArmMessage, // ダメージ(切り刻み)
        CutUpLegMessage, // ダメージ(切り刻み)
        CutUpStomachMessage, // ダメージ(切り刻み)
        FrameThrowMessage, // ダメージ(火炎放射)
        InvasiveMouthMessage, // ダメージ(粘液)
        NumbLiquidMessage, // ダメージ(痺れる粘液)
        OnikoroshiMessage, // ダメージ(鬼殺し)
        PunchStomachMessage, // ダメージ(殴りつけ)
        StringerMessage, // ダメージ(毒針)
        AttackDescription, // 説明文(ATTACK)
        DefenceDescription, // 説明文(DEFENCE)
        FatalitySkillDescription, // 説明文(FATALITY)
        FieldRationDescription, // 説明文(レーション)
        FirstAidDescription, // 説明文(ファーストエイド)
        HonzougakuDescription, // 説明文(本草学)
        IshinhouDescription, // 説明文(医心方)
        KuchiyoseDescription, // 説明文(口寄せ)
        KyoukasuigetsuDescription, // 説明文(鏡花水月)
        MurasameDescription, // 説明文(村雨)
        MusterStrengthDescription, // 説明文(力溜め)
        NadegiriDescription, // 説明文(撫斬)
        RaikiriDescription, // 説明文(雷切)
        RandomShotsDescription, // 説明文(ランダムショット)
        ShichishitouDescription, // 説明文(七支刀)
        SilverBulletDescription, // 説明文(シルバーバレット)
        SkillDescription, // 説明文(SKILL)
        SmokeBombDescription, // 説明文(発煙筒)
        StarShellDescription, // 説明文(照明弾)
        TaserGunDescription, // 説明文(テイザーガン)
        UtsusemiDescription, // 説明文(空蝉)
        WabisukeDescription // 説明文(侘助)
    }
}